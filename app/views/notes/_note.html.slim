tr{class="#{cycle('even', 'odd')}"}
  td{width="50%" class="emphasize"} = link_to note.title, project_note_path(@project,note)
  td = "created #{time_ago_in_words(note.created_at)} ago"
  td = link_to "delete", project_note_path(@project,note), :class => "btn", :confirm => "Sure?", :method => :delete